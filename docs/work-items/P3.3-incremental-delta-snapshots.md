# P3.3 — Incremental and Delta Snapshots

Status: Implemented and verified on 2026-09-20

Phase: 3 — Ecosystem, reliability, and evaluation

Required approver: Repository maintainer

## Problem and user outcome

Clients currently need a complete snapshot after every observable change. Large
surfaces need a bounded incremental representation that preserves the exact
surface and revision boundary without turning partial state into action
authority. A client must also know when it cannot safely continue and must
request a complete snapshot.

## Dependencies and scope

P3.2 must first freeze the shared lifecycle and Playwright revision semantics.
This work then defines an additive, separately versioned delta contract,
deterministic snapshot diff/apply helpers, explicit gap and resynchronization
behavior, generated schema, fixtures, and an optional package subpath.

The accepted design in [ADR 0009](../adr/0009-incremental-delta-contract.md)
uses full-node upserts and removals rather than generic JSON Patch paths.
Revisions remain opaque equality tokens. A delta binds one `surfaceId`, one
exact `baseRevision`, one target `revision`, and canonical SHA-256 digests of
the exact base and target snapshots.

## Non-goals and prohibited changes

- no change to the frozen Phase 1 root exports, declarations, or five schemas;
- no authorization or execution directly from a partial delta;
- no assumption that revision strings are numeric, ordered, or globally unique;
- no lossy merge, best-effort application, cross-surface application, or silent
  recovery from a missing base;
- no streaming transport, persistence service, compression dependency, or
  Phase 4 protocol.

## Trust boundaries and data classification

Delta producers, serialized deltas, cached bases, and transport order are
untrusted. Application requires a locally validated exact base. Redaction is
performed before diffing; removed or replaced sensitive state must not survive
through stale fields. A reconstructed snapshot remains observation data and
must be revalidated against the current executing surface.

## Compatibility and rollback

The contract is additive and separately versioned. Consumers that do not import
the optional subpath continue using complete snapshots. Reverting the subpath,
schema, fixtures, and documentation restores the previous behavior without
data migration.

## Acceptance criteria

| ID | Requirement |
|---|---|
| AC-01 | Applying a valid delta to its exact base reconstructs the target snapshot after deterministic canonicalization. |
| AC-02 | The format binds schema version, surface, base revision, target revision, and generation time without ordering assumptions about revisions. |
| AC-03 | Wrong surface/base, gaps, duplicate IDs, contradictory operations, malformed records, and out-of-order application fail closed and request full resynchronization where applicable. |
| AC-04 | Node upserts/removals and changed top-level fields have deterministic ordering and bounded counts and serialized size. |
| AC-05 | Diff and apply never mutate caller-owned snapshots or delta values. |
| AC-06 | Reconstructed snapshots pass the existing snapshot validator; invalid nested nodes/actions cannot enter through a delta. |
| AC-07 | Sensitive removals and replacements leave zero secret bytes in the delta and reconstructed target. |
| AC-08 | A delta cannot be supplied as current execution authority; retained requests remain subject to current surface revision checks. |
| AC-09 | Round-trip, property-style, malformed-corpus, budget, and secret tests are deterministic. |
| AC-10 | Typecheck, build, full deterministic gate, frozen Phase 1 contract, audit, package inspection, isolated consumer, and diff checks pass. |

## Implemented design and evidence

Source is `src/delta/`, exported only through
`dual-surface-ui/delta`, with `schemas/phase3/agent-snapshot-delta-0.1.schema.json`
and fixtures kept distinct from the frozen Phase 1 manifest. The contract uses
complete target metadata, sorted full-node upserts, ID-only removals, exact
base/target digests, descriptor-safe input capture, and fixed resynchronization
outcomes.

The initial limits are 4,096 snapshot nodes, 512 combined delta operations,
64 actions per node, 1,024 actions total, 256 capabilities, 1 MiB per captured
snapshot, and 1 MiB per delta. Tests cover identical snapshots, metadata-only
changes, node/action changes, removal, reorder normalization, multi-delta
chains, missed updates, tampering, prototype/accessor input, secret removal,
and boundary budget exhaustion.

The implementation is commit `ccc20c0`. Evidence was collected on Windows with
Node `v24.16.0` and npm `11.13.0`:

| Evidence | Result |
|---|---|
| `npx vitest run test/delta.test.ts` | 16/16 passed, including exact round trip, generated-time no-change, collision, cross-realm values, stale/gapped chains, digest tampering, fixed-seed corpus, secret removal/replacement, malformed input, immutability, and budget boundaries. |
| exact VM gate invocation | 16/16 passed with `vmThreads`, one worker, no file parallelism, and isolation disabled. |
| `npm run test:gate` | 31 files and 388 tests passed. |
| `npm run typecheck` | TypeScript, generated schemas, and Angular compilation passed. |
| `npm run contract:check` | Frozen Phase 1 contract remained 32 runtime exports, 6 declaration files, and 5 schemas. |
| `npm run package:check:delta` | Packed root and delta imports, declarations, generated-schema equality and AJV compilation, round trip, stale-base resync, and frozen-root count passed. |
| `npm run package:check:playwright` | Existing optional Playwright packed consumers still passed with Playwright 1.63.0. |
| `npm audit --json` | 0 vulnerabilities at every severity. |
| `git diff --check` | Passed; only Windows line-ending notices were emitted. |

Independent security review found no unresolved Critical, High, or Medium
issue. A separate correctness/package review initially found inconsistent
budget reasons, incomplete adversarial evidence, and shallow packed-schema
verification. Those findings were fixed and the reviewer confirmed all three
resolved before the implementation commit.

The delta is observation-only. The subpath exports no execution primitive and
does not alter the existing live-surface lifecycle, so retained action requests
still require current live revision and action metadata.

## Traceability

| Goal | Acceptance criteria | Implemented source | Deterministic evidence |
|---|---|---|---|
| Exact incremental state | AC-01–AC-06 | `src/delta/` | diff/apply, digest, chain, cross-realm, malformed, and fixed-seed tests |
| Preserve safety | AC-07–AC-08 | canonical capture and atomic apply | secret removal/replacement, accessor and pollution tests, plus subpath/API review |
| Preserve package | AC-09–AC-10 | `./delta`, Phase 3 schema, package scripts | frozen contract, compiled packed schema, consumer, audit, and full gates |

All AC-01 through AC-10 are satisfied. P3.4 is next; Phase 3 Exit Gate remains
open through P3.10.
